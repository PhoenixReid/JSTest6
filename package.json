{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "first tests with Cypress",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:headed:laptop": "npx cypress open --config-file cypress.config.laptop.js",
    "test:headless:laptop": "npx cypress open--config-file cypress.config.laptop.js --browser chrome",
    "test:headed:mobile": "npx cypress open --config-file cypress.config.mobile.js",
    "test:headless:mobile": "npx cypress open --config-file cypress.config.mobile.js --browser electron"
  },
  "author": "OM",
  "license": "ISC",
  "dependencies": {
    "cypress": "^8.3.0"
  }
}
